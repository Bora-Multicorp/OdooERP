<?xml version="1.0"?>
<odoo>
    <record id="reject_sale_order_wizard_view_form" model="ir.ui.view">
        <field name="name">reject.sale.order.wizard.form</field>
        <field name="model">reject.sale.order.wizard</field>
        <field name="arch" type="xml">
            <form string="Rejection SaleConfirmation Request">
                <group>
                    <field name="remark" placeholder="Enter remarks for sale order rejection"/>
                </group>
                <footer>
                    <button name="action_reject_so_confirm" string="Reject" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="reject_so_confirm_wizard_action" model="ir.actions.act_window">
        <field name="name">Reject Sale Confirmation Request</field>
        <field name="res_model">reject.sale.order.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="reject_sale_order_wizard_view_form"/>
        <field name="target">new</field>
        <!-- <field name="binding_model_id" ref="model_approve_po_unlock_wizard"/> -->
        <field name="context">{
            'default_sale_id': active_id,
            }
        </field>
    </record>
</odoo>