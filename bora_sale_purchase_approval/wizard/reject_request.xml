<?xml version="1.0"?>
<odoo>
      <record id="reject_sale_order_wizard_view_form" model="ir.ui.view">
        <field name="name">reject.sale.order.wizard.form</field>
        <field name="model">reject.sale.order.wizard</field>
        <field name="arch" type="xml">
            <form string="Reject Sales Order Form">
                <group>
                    <field name="sale_order_id" readonly="1" invisible="1"/>
                    <field name="remark" placeholder="Enter remarks for rejection"/>
                </group>
                <footer>
                    <button name="action_reject_sale_order" string="Reject" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Window Action that opens the Sales Order Rejection Wizard -->
    <record id="reject_sale_order_wizard_action" model="ir.actions.act_window">
        <field name="name">Reject Sales Order</field>
        <field name="res_model">reject.sale.order.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="reject_sale_order_wizard_view_form"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="context">{
            'default_sale_order_id': active_id,
        }
        </field>
    </record>
</odoo>