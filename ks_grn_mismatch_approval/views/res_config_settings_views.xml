<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_inherit_ks_grn_mismatch" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.ks.grn.mismatch</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='operations_setting_container']" position="after">
                <block title="GRN Mismatch Approval" name="ks_grn_mismatch_block" groups="stock.group_stock_manager">
                    <setting id="ks_grn_responsible_setting" company_dependent="1" string="GRN / Receipt Responsible User"
                             help="User to notify when a GRN mismatch is approved (e.g. warehouse responsible).">
                        <field name="ks_grn_responsible_user_id" options="{'no_create': True}"
                               placeholder="Select user to notify on approval..."/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
