<odoo>
    <record id="view_purchase_order_backdate" model="ir.ui.view">
        <field name="name">view.purchase.order.backdate</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='date_order']" position="before">
                <field name="create_date" string="Creation Date" required="1" readonly="state == 'done'"/>
            </xpath>
        </field>
    </record>

    <!-- <template id="report_purchaseorder_document_inherit_po_date" inherit_id="purchase.report_purchaseorder_document">
        <xpath expr="//div[@class='mt-4']/t[@t-set='layout_document_title']" position="after">
            <div class="mt-1">
                <strong>PO Date: </strong>
                <span t-esc="o.create_date and o.create_date.strftime('%d-%m-%Y %H:%M') or ''"/>
            </div>
        </xpath>
    </template> -->

</odoo>