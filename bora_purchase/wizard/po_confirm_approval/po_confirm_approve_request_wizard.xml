<?xml version="1.0"?>
<odoo>
    <record id="confirm_po_wizard_view_form" model="ir.ui.view">
        <field name="name">confirm.po.wizard.form</field>
        <field name="model">confirm.po.wizard</field>
        <field name="arch" type="xml">
            <form string="Approval Confirmation Request">
                <group> 
                    <field name="remark" placeholder="Enter remarks for purchase order confirmation"/>
                </group>
                <footer>
                    <button name="action_approve_po_confirm" string="Approve" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form> 
        </field>
    </record>
    <record id="approve_po_confirm_wizard_action" model="ir.actions.act_window">
        <field name="name">Approve Confirmation Request</field>
        <field name="res_model">confirm.po.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="confirm_po_wizard_view_form"/>
        <field name="target">new</field>
        <field name="context">{
            'default_order_id': active_id,
            }
        </field>
    </record>
</odoo>
