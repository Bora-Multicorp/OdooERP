<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Sale Approval Manager Group (PM Users) -->
        <record id="group_ks_sale_approval_manager" model="res.groups">
            <field name="name">Sale Approval Manager (PM)</field>
            <field name="category_id" ref="base.module_category_sales"/>
<!--            <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>-->
            <field name="comment">Users with this role can approve or reject Sale Order confirmation and cancellation requests. They can also edit prices and confirm orders directly.</field>
        </record>

        <!-- Add PM group to Sale Manager for convenience -->
        <record id="sales_team.group_sale_manager" model="res.groups">
            <field name="implied_ids" eval="[(4, ref('group_ks_sale_approval_manager'))]"/>
        </record>
    </data>

    <data noupdate="1">
        <!-- Record rules for approval config - only managers can modify -->
        <record id="ks_sale_approval_config_rule_user" model="ir.rule">
            <field name="name">KS Sale Approval Config: Read for Sale Users</field>
            <field name="model_id" ref="model_ks_sale_approval_config"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="ks_sale_approval_config_rule_manager" model="ir.rule">
            <field name="name">KS Sale Approval Config: Full Access for Managers</field>
            <field name="model_id" ref="model_ks_sale_approval_config"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>

