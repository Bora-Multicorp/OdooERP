<?xml version="1.0"?>
<odoo>
    <record id="confirm_suspend_vendor_wizard_view_form" model="ir.ui.view">
        <field name="name">confirm.suspend.vendor.wizard.view.form</field>
        <field name="model">suspend.vendor.confirm.wizard</field>
        <field name="arch" type="xml">
            <form string="Approval Suspend">
                <group> 
                    <div class="o_form_label o_text_warning" style="margin-bottom: 10px;">
                            Are you sure you want to suspend this KYC approval process?
                    </div>
                    <field name="remark" placeholder="Enter remarks for suspension"/>
                </group>
                <footer>
                    <button name="action_done" string="Suspend" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form> 
        </field>
    </record>
    <record id="approve_suspend_vendor_confirm_wizard_action" model="ir.actions.act_window">
        <field name="name">Suspend Confirmation Request</field>
        <field name="res_model">suspend.vendor.confirm.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="confirm_suspend_vendor_wizard_view_form"/>
        <field name="target">new</field>
        <field name="context">{
            'default_kyc_id': active_id,
            }
        </field>
    </record>
</odoo>
