<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Purchase Approval Manager Group -->
        <record id="group_ks_purchase_approval_manager" model="res.groups">
            <field name="name">Purchase Approval Manager (PM)</field>
            <field name="category_id" ref="base.module_category_inventory_purchase"/>
            <field name="implied_ids" eval="[(4, ref('purchase.group_purchase_user'))]"/>
            <field name="comment">Users with this role can approve or reject Purchase Order confirmation, update, and cancellation requests.</field>
        </record>

        <!-- Add PM group to Purchase Manager for convenience -->
        <record id="purchase.group_purchase_manager" model="res.groups">
            <field name="implied_ids" eval="[(4, ref('group_ks_purchase_approval_manager'))]"/>
        </record>
    </data>

    <data noupdate="0">
        <!-- Record rules for approval config - only managers can modify -->
        <record id="ks_purchase_approval_config_rule_user" model="ir.rule">
            <field name="name">KS Purchase Approval Config: Read for Purchase Users</field>
            <field name="model_id" ref="model_ks_purchase_approval_config"/>
            <field name="groups" eval="[(4, ref('purchase.group_purchase_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="ks_purchase_approval_config_rule_admin" model="ir.rule">
            <field name="name">KS Purchase Approval Config: Full Access for Admins</field>
            <field name="model_id" ref="model_ks_purchase_approval_config"/>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>

