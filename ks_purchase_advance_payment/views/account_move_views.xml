<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- For vendor bills: hide Pay until PO has approved "with bill" payment request; show Request For Payment instead. -->
    <record id="view_move_form_inherit_vendor_payment_approval" model="ir.ui.view">
        <field name="name">account.move.form.vendor.payment.approval</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Pay button: also hide for vendor bills (in_invoice) when linked PO has no approved payment request -->
            <xpath expr="//button[@name='action_register_payment'][@id='account_invoice_payment_btn']" position="attributes">
                <attribute name="invisible">(
                    state != 'posted'
                    or payment_state not in ('not_paid', 'partial', 'in_payment')
                    or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')
                    or invoice_has_outstanding
                ) or (move_type == 'in_invoice' and not ks_can_register_vendor_payment)</attribute>
            </xpath>
            <xpath expr="//button[@name='action_register_payment'][@id='account_invoice_payment_secondary_btn']" position="attributes">
                <attribute name="invisible">(
                    state != 'posted'
                    or payment_state not in ('not_paid', 'partial', 'in_payment')
                    or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')
                    or not invoice_has_outstanding
                ) or (move_type == 'in_invoice' and not ks_can_register_vendor_payment)</attribute>
            </xpath>
            <!-- Request For Payment: visible for posted vendor bills with linked PO(s); hidden when payment is already allowed (approved) -->
            <xpath expr="//button[@name='action_register_payment']" position="before">
                <button name="action_create_bill_payment_request"
                        type="object"
                        string="Request For Payment"
                        class="btn-secondary"
                        invisible="move_type != 'in_invoice' or state != 'posted' or ks_can_register_vendor_payment or not ks_has_linked_po"
                        help="Create or open a payment approval request for this vendor bill. Without approval, the Pay button will not be available. After approval, you can register payment."/>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="ks_can_register_vendor_payment" invisible="1"/>
                <field name="ks_has_linked_po" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>
