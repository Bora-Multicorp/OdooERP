<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="email_template_sla_breach" model="mail.template">
        <field name="name">Vendor SLA Breach Notification</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="subject">🚨 SLA Breach Alert – Vendors & Purchase Orders</field>
        <field name="email_from">${(user.email or '')|safe}</field>
        <field name="body_html" type="html">
            <![CDATA[
        <p>Hello Procurement Team,</p>

        <p>The following <b>Purchase Orders have breached Vendor SLA timelines</b>:</p>

        <table border="1" cellpadding="6" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th>Vendor</th>
                    <th>Purchase Order</th>
                    <th>Approved Date</th>
                    <th>Receipt Date</th>
                    <th>SLA Days</th>
                </tr>
            </thead>
            <tbody>
                % for po in ctx.get('breached_pos', []):
                <tr>
                    <td>${po.partner_id.name}</td>
                    <td>${po.name}</td>
                    <td>${po.date_approve}</td>
                    <td>${po.picking_ids.filtered(lambda p: p.state == 'done')[:1].date_done}</td>
                    <td>${po.partner_id.sla_lead_time_days}</td>
                </tr>
                % endfor
            </tbody>
        </table>

        <p>Please review and take necessary action.</p>

        <p>Thanks,<br/>Odoo System</p>
        ]]>
        </field>
    </record>
</odoo>