<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="bora_vendor_bill_approval_reason_wizard_form" model="ir.ui.view">
        <field name="name">bora.vendor.bill.approval.reason.wizard.form</field>
        <field name="model">bora.vendor.bill.approval.reason.wizard</field>
        <field name="arch" type="xml">
            <form string="Approval / Rejection Reason">
                <field name="bora_action_type" invisible="1"/>
                <field name="bora_is_rejection" invisible="1"/>
                <group>
                    <field name="bora_move_id" readonly="1"/>
                    <field name="bora_action_label" string="Action" readonly="1"/>
                    <field name="bora_reason" placeholder="Please enter a reason (required)..."/>
                </group>
                <footer>
                    <button name="action_submit" type="object" class="btn-danger"
                            string="Confirm Rejection" invisible="not bora_is_rejection"/>
                    <button name="action_submit" type="object" class="btn-success"
                            string="Confirm Approval" invisible="bora_is_rejection"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
