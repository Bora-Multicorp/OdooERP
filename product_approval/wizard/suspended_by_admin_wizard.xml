<?xml version="1.0"?>
<odoo>
    <record id="confirm_product_wizard_view_form" model="ir.ui.view">
        <field name="name">confirm.product.wizard.view.form</field>
        <field name="model">product.suspend.wizard</field>
        <field name="arch" type="xml">
            <form string="Approval Suspend">
                <group> 
                    <div class="o_form_label o_text_warning" style="margin-bottom: 10px;">
                            Are you sure you want to suspend this product approval process?
                    </div>
                    <field name="remark" placeholder="Enter remarks for suspension"/>
                </group>
                <footer>
                    <button name="action_done" string="Suspend" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x"/>
                </footer>
            </form> 
        </field>
    </record>
    <record id="suspend_product_approval_wizard_action" model="ir.actions.act_window">
        <field name="name">Suspend Confirmation Request</field>
        <field name="res_model">product.suspend.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="confirm_product_wizard_view_form"/>
        <field name="target">new</field>
        <field name="context">{
            'default_product_id': active_id,
            }
        </field>
    </record>
</odoo>
